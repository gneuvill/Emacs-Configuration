;; Emacs Code Brower
(add-to-list 'load-path (concat grail-dist-elisp "ecb"))
;; CEDET => we use the one shipped with emacq
;; (add-to-list 'load-path (concat grail-dist-elisp "cedet"))
;; jde
;; (add-to-list 'load-path (concat grail-dist-elisp "jde/lisp"))
;; malabar-mode => should replace this with emacs-eclim
; (add-to-list 'load-path (concat grail-dist-elisp "malabar-1.5-SNAPSHOT/lisp"))
;; BBDB
(add-to-list 'load-path (concat grail-dist-elisp "bbdb/lisp"))
;; Scala
;; (add-to-list 'load-path (concat grail-dist-elisp "scala"))
;; Ensime (for Scala)
;; (add-to-list 'load-path (concat grail-dist-elisp "ensime/elisp/"))
